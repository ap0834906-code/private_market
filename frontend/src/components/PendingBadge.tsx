const PendingBadge = () => (
  <span className="brutal-btn bg-secondary text-secondary-foreground px-3 py-1 text-xs animate-pulse inline-block">
    PENDING...
  </span>
);

export default PendingBadge;
